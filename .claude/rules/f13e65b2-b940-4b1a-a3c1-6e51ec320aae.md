<rule_activation id="f13e65b2-b940-4b1a-a3c1-6e51ec320aae" title="Standardize Testing Fixtures for Public API Validation: Versioned Implementations Maintain" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching the public API surface, versioned implementations, and internal support modules.
</rule_activation>

### Rules

- **R-FIXTURE-001** MUST: Versioned API implementations (e.g., v1, v2) MUST maintain separate fixture sets to validate version-specific behavior

### Verify

```bash
# Count test functions across the codebase
grep -r 'def test_' tests/ | wc -l

# Find pytest fixture definitions
find tests/ -name 'conftest.py' -o -name 'fixtures.py' | xargs grep -l '@pytest.fixture'

# Count tests related to networks, schema, and model modules
pytest --collect-only -q | grep -E '(networks|schema|model)' | wc -l
```

**Accept when:**
- All public API modules have corresponding test files with fixture-based validation
- Test coverage for public API modules exceeds 80% with fixtures covering both happy path and edge cases
- Versioned API implementations have separate fixture sets that validate version-specific behavior

<enforcement>
Claude Code MUST NOT skip or defer verification. All public API changes require corresponding fixture-based tests. Coverage drops below 80% threshold trigger build failures. Pull requests without fixtures are blocked from merge.
</enforcement>