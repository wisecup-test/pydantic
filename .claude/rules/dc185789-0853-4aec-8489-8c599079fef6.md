<rule_activation id="dc185789-0853-4aec-8489-8c599079fef6" title="Adopt Comprehensive Test Suite Organization for Core Library Validation: Test Suites Include" applies_to="tests/**/*.py">
These rules are ALWAYS ACTIVE for all test files within the pydantic-core/tests directory. Test suites must follow a hierarchical organizational pattern by functional domain (validators, serializers, arguments) with comprehensive coverage across validation modes and type handling.
</rule_activation>

### Rules

- **R-TSO-001** MUST: Test suites MUST include coverage for both strict and lax validation modes where applicable

### Verify

```bash
# Count test files in standard domain directories
find tests/ -type f -name 'test_*.py' | grep -E 'tests/(validators|serializers|arguments)/' | wc -l

# Collect all test modules
pytest tests/ --collect-only --quiet | grep -c '<Module'

# Execute full test suite with verbose output
pytest tests/ -v --tb=short --maxfail=1
```

**Accept when:**
- All test files follow the naming convention `test_<component>.py` and are organized in appropriate domain subdirectories (validators/, serializers/, arguments/)
- Test discovery finds all test files and pytest can execute the full suite without import errors
- CI/CD pipeline successfully executes tests with clear pass/fail status and coverage reports
- Coverage reports show validators without corresponding test files are identified and tracked
- Tests are tagged with pytest markers (@pytest.mark) for selective execution (unit, integration, slow)

<enforcement>
Claude Code MUST NOT skip or defer verification. All test files must be validated against organizational structure and naming conventions before acceptance. Pull requests with incorrectly organized tests must receive automated guidance. New validator additions without corresponding tests must block merge until tests are added.
</enforcement>