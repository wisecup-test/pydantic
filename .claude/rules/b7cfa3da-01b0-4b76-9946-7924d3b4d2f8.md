<rule_activation id="b7cfa3da-01b0-4b76-9946-7924d3b4d2f8" title="Standardize Testing Fixtures for Public API Validation: Breaking Changes Public" applies_to="**/*">
These rules are ALWAYS ACTIVE for all files matching public API surface, versioned implementations, and internal implementation modules that directly support public APIs.
</rule_activation>

### Rules

- **R-API-001** MUST: Breaking changes to public API behavior MUST be detected through fixture-based regression tests before release.

### Verify

```bash
# Count test functions in test suite
grep -r 'def test_' tests/ | wc -l

# Find pytest fixture definitions
find tests/ -name 'conftest.py' -o -name 'fixtures.py' | xargs grep -l '@pytest.fixture'

# Count tests related to networks, schema, and model modules
pytest --collect-only -q | grep -E '(networks|schema|model)' | wc -l
```

**Accept when:**
- All public API modules have corresponding test files with fixture-based validation
- Test coverage for public API modules exceeds 80% with fixtures covering both happy path and edge cases
- Versioned API implementations have separate fixture sets that validate version-specific behavior
- Fixture organization follows functional domain structure (fixtures/networks/, fixtures/schema/, fixtures/models/)
- All public API changes include corresponding fixture-based tests before merge

<enforcement>
Claude Code MUST NOT skip or defer verification. Pull requests adding or modifying public APIs without corresponding fixtures are blocked from merge. Coverage drops below 80% threshold trigger build failures and require remediation before release.
</enforcement>